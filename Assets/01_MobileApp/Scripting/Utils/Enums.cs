using UnityEngine;

public class Enums : MonoBehaviour
{
    public enum States { Happy, Sad }
    public enum Rarities { Common, Rare, Epic, Legendary }
    public enum SkinTypes { Hat, Glasses, Scarf, Stem, Pot, Flower }
}
